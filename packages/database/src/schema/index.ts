// Schema barrel — add new table modules here
// e.g.: export * from './users.js';
//       export * from './sites.js';

// Placeholder so the package compiles before any real tables are defined.
export const _placeholder = true;
