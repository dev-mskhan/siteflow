// apps/server/src/middleware/index.ts
// Barrel for Fastify hooks & middleware plugins.
// Each middleware is a FastifyPlugin that can be registered via app.register().
//
// Add middleware here as the app grows:
//   export * from './auth.js';
//   export * from './rate-limit.js';
//   export * from './request-context.js';
